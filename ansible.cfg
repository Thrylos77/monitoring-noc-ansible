[defaults]
inventory       = inventory/hosts.ini
roles_path      = roles/common:roles/infra:roles/server:roles/agent
vault_password_file = .vault_pass
host_key_checking = False
pipelining        = True
forks             = 5

[privilege_escalation]
become        = True
become_method = sudo
become_user   = root
